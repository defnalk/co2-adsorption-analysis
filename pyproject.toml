[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.coverage.run]
source = ["co2_adsorption_analysis"]
omit = ["tests/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if __name__ == .__main__.:",
]
